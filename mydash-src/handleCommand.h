#ifndef __HANDLECOMMAND_H
#define __HANDLECOMMAND_H
/**
 * Handles foreground commands given by a user
 * @param command command entered
 * @param token tokenized array of user input
 */
void handleCommand(char *command, char **token);

#endif  /* __HANDLECOMMAND_H */
